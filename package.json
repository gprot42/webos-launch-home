{
  "name": "webos-launch-home",
  "version": "0.0.1",
  "description": "Launch Home — fullscreen home screen for rooted LG webOS TVs",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "pack": "node scripts/build.js --pack",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@procot/webostv": "^1.2.6",
    "@webos-tools/cli": "^3.2.5",
    "esbuild": "^0.25.0"
  }
}
